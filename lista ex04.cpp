#include <iostream>
using namespace std;

int main() {
    float base, altura;
    cout << "Digite a base e a altura do triângulo: ";
    cin >> base >> altura;
    cout << "Área: " << (base * altura) / 2 << endl;
    return 0;
}