#include <iostream>
using namespace std;

int main() {
    float a, b, c, d;
    cout << "Digite 4 valores: ";
    cin >> a >> b >> c >> d;
    cout << "Média: " << (a + b + c + d) / 4 << endl;
    return 0;
}
